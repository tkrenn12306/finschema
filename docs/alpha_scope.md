# finschema v0.1.0 Alpha Scope

This release intentionally implements only the alpha-compatible surface:

- Core financial value types in `finschema.types`
- Static reference data in `finschema.reference`
- Single-value CLI checks via `finschema check`

All other top-level modules are scaffolded placeholders for later releases.
